<!--------------------------------- Navbar Search ----------------------------------->
<nav class="navbar-search">
  <div class="container-fluid">
    <form class="d-flex" role="search">
      <input class="form-control me-2" type="search" placeholder="Search Job">
    </form>
  </div>
</nav>


<nav class="navbar-filtros">
  <div class="container-fluid">
    <form class="d-flex">
      <div class="col-3">
        <div class="dropdown-center">
          <button class="dropdown-toggle" type="button" data-bs-toggle="dropdown">Fecha</button>
        </div>
      </div>
      <div class="col-3">
      <div class="dropdown-center">
        <button class="dropdown-toggle" type="button">Filtros</button>
      </div>
    </div>
    </form>
  </div>
</nav>




<div id="jobs">
  <% @jobs.each do |job| %>
    <a href=<%=job_path(job)%> class="card-link">
      <!------ Note: If you prefeer could use the method <%=job_card(job)%> to render this component ------>
      <!------------------------------------ Card  --------------------------------------->
      <div class="card">
        <div class="row g-0 align-items-center">
          <!--------------------------  Image  -------------------------->
          <div class="col-2 d-flex justify-content-center">
            <%= image_tag "icons/company-bbb.png"%>
          </div>
          <!--------------------------  Body  -------------------------->
          <div class="col-10">
            <div class="card-body">
              <!-------------- Title  ---------------->
              <h5 class="card-title"><%= job.title %></h5>
              <!-------------- Subtitle  ---------------->
              <h6 class="card-subtitle mb-2 text-body-secondary"><%= job.company.name %></h6>

              <!-------------- Secondary  ---------------->
              <p class="card-text" style="line-height: 1;">
                <small class="text-body-secondary"> <%= job.location %> <br class="small"> <%= job.location_mode %> </small>
              </p>
              <span class="card-text" style="float: left;">
                <small class="text-body-secondary"><%= job.date %></small>
              </span>
              <span class="card-text" style="float: right;">
                <small class="text-body-secondary"> $<%= job.salary %></small>
              </span>
            </div>
          </div>
        </div>
      </div>
    </a>
  <% end %>

</div>

<%= link_to "New job", new_job_path %>
